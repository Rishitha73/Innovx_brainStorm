# Submitted Applications

The mock portal stores submitted application records in browser `localStorage` under `innovx-submitted-applications` so they can be reviewed without a backend.

This folder documents the local review surface. Uploaded document bytes are not written here; the demo keeps only application metadata and the derived filename in the browser session. 

